# ── Telegram ──────────────────────────────────────────────────────────────────
# Get token from @BotFather
TELEGRAM_API_KEY=your-telegram-bot-token

# Comma-separated list of Telegram chat IDs the bot is allowed to work in.
# Channel IDs are negative numbers starting with -100, e.g. -1001234567890.
# To find the ID: add the bot to the channel, send any message, check logs for "chat_id".
# Leave empty to disable the restriction (not recommended in production).
ALLOWED_CHAT_IDS=-1001234567890

# ── MongoDB ───────────────────────────────────────────────────────────────────
# Use "mongodb" hostname inside Docker, "localhost" outside
MONGODB_URI=mongodb://easygo_user:easygo_pass@mongodb:27017/easygo_bot?authSource=admin

# MongoDB init credentials (used by docker-compose to create the DB)
MONGO_INITDB_ROOT_USERNAME=easygo_user
MONGO_INITDB_ROOT_PASSWORD=easygo_pass
MONGO_INITDB_DATABASE=easygo_bot

# ── Google Sheets ──────────────────────────────────────────────────────────────
# The spreadsheet ID from its URL:
#   https://docs.google.com/spreadsheets/d/<SPREADSHEET_ID>/edit
GOOGLE_SHEET_ID=your-spreadsheet-id

# Path to the GCP Service Account JSON key (mounted into the container)
GOOGLE_CREDENTIALS_PATH=credentials.json

# Name of the worksheet used for step tracking (default: Шаги)
# WORKSHEET_NAME=Шаги

# ── Medals ─────────────────────────────────────────────────────────────────────
# Channel ID where the daily medal summary is posted (e.g. -1001234567890).
# Leave unset to disable medal reports.
# REPORT_CHANNEL_ID=-1001234567890

# ── Logging ───────────────────────────────────────────────────────────────────
LOG_LEVEL=INFO

# OpenAI settings
OPENAI_API_KEY=your_openai_api_key
OPENAI_MODEL=gpt-4o-mini
